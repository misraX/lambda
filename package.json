{
  "name": "serverless-geo",
  "version": "0.0.0",
  "description": "Serverless geo",
  "scripts": {
    "deploy": "serverless deploy",
    "format": "prettier --write --single-quote --print-width=120 --parser=flow --tab-width=2 \"**/*.{js,jsx}\"",
    "test": "NODE_ENV=test mocha --exit --recursive \"tests/*.spec.js\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.6.7",
    "busboy": "^0.3.1",
    "geopoint": "^1.0.1",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "node-fetch": "^2.6.0",
    "prettier": "^1.18.2"
  }
}